name: Security vulnerability scan

on:
    workflow_dispatch:
    pull_request:

jobs:
    scan:
        name: Secscan
        uses: canonical/observability/.github/workflows/charm-sec-scan.yaml@feat/secscan-test
        secrets: inherit
