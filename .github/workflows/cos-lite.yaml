name: Upgrade product module

on:
  pull_request:
    branches:
      - main
    paths:
      - "**/*.tf"

jobs:
  upgrade:
    name: Upgrade
    uses: canonical/o11y-tester-operator/.github/workflows/_upgrade_product.yaml@test/mt
    with:
      product: cos-lite
      pytest-file: test_upgrade_1_to_2.py
    secrets: inherit
