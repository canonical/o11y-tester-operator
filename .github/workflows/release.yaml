name: Release

on:
  push:
    branches:
      - main
      - track/0

jobs:
  release:
    # TODO: revert this to main when #30 goes through.
    uses: canonical/observability/.github/workflows/charm-release.yaml@copilot/use-charmcraft-remote-build
    secrets: inherit
