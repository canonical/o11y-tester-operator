name: Release

on:
  push:
    branches:
      - main
      - track/0

permissions:
  contents: write

jobs:
  release:
    uses: canonical/observability/.github/workflows/charm-release.yaml@feature/v1
    secrets: inherit
