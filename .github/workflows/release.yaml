name: Release

on:
  pull-request:
    branches:
      - main
      - track/0

jobs:
  release:
    uses: canonical/observability/.github/workflows/charm-release.yaml@ci/release-remote-build
    secrets: inherit
